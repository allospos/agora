%h5
  =link_to "Products", products_path
%h4
  =@product.title

.row
  .large-4.columns
    %img.thumbnail{ src: "http://placekitten.com/g/500/400"}
  .large-5.columns
    %p= @product.description
  .large-3.columns
    .callout
      %p.text-center.stat
        &euro;#{@product.price}
      =link_to "Add to cart", "#", class: "button expanded"
    .callout
      %h5 Seller information
      = @product.user.username
      %br
      %br
      %b Verified information
      %ul.no-bullet
        %li Email address
        %li Phone number

    %small added at #{l(@product.created_at)}
